// eslint-disable-next-line camelcase
import { Lexend, Archivo_Black } from 'next/font/google'

const font = Lexend({ weight: '300', subsets: ['latin'] })
const title = Archivo_Black({ weight: '400', subsets: ['latin'] })

export { font, title }
